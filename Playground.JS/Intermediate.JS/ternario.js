let isValid = true;

function verify(isValid) {
    if (isValid) {
        return true;
    } else {
        return false;
    }
}

//Com ternario
const result =  isValid ? true : false;

let zero = 0;

const nameResult = zero == 0 ? 0 : -1;

console.log(nameResult);

